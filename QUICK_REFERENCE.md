# 📋 বাংলাস্ক্রিপ্ট কুইক রেফারেন্স কার্ড
# BanglaScript Quick Reference Card

---

## 🔤 ভেরিয়েবল (Variables)

```banglascript
ধ্রুবক নাম = "রহিম";      // const - পরিবর্তন হবে না
চলক বয়স = ২৫;           // let - পরিবর্তন হতে পারে
সংখ্যা টাকা = ১০০;       // let
পরিবর্তনশীল x = ১০;      // var (পুরাতন)
ব্যাক্তি শহর = "ঢাকা";    // const
বাক্য বার্তা = "হ্যালো";    // let
```

---

## 📊 ডাটা টাইপ (Data Types)

```banglascript
// বুলিয়ান
সত্য              // true
মিথ্যা            // false

// শূন্য মান
শূন্য             // null
অনির্ধারিত        // undefined

// টাইপ চেক
ধরন নাম          // typeof
```

---

## 🔀 শর্ত (Conditions)

```banglascript
যদি (শর্ত) {
    // সত্য হলে
} নাহলে যদি (অন্য_শর্ত) {
    // এটা সত্য হলে
} নাহলে {
    // সব মিথ্যা হলে
}
```

### তুলনা অপারেটর
| বাংলা | চিহ্ন |
|-------|-------|
| সমান | === |
| অসমান | !== |
| বড় | > |
| ছোট | < |
| বড়_বা_সমান | >= |
| ছোট_বা_সমান | <= |
| এবংএবং | && |
| অথবাঅথবা | \|\| |
| না/নয় | ! |

---

## 🔁 লুপ (Loops)

```banglascript
// for লুপ
জন্য (সংখ্যা i = ০; i < ৫; i++) {
    লিখো(i);
}

// while লুপ
যখন (শর্ত) {
    // কোড
}

// do-while লুপ
করো {
    // কোড
} যখন (শর্ত);

// for...of
জন্য (ধ্রুবক আইটেম অফ তালিকা) {
    লিখো(আইটেম);
}

// for...in
জন্য (ধ্রুবক key ইন অবজেক্ট) {
    লিখো(key);
}
```

### লুপ কন্ট্রোল
```banglascript
থামাও           // break
চালিয়ে_যাও      // continue
```

---

## ⚡ ফাংশন (Functions)

```banglascript
// সাধারণ ফাংশন
অনুষ্ঠান নাম(প্যারামিটার) {
    // কোড
    প্রেরণ মান;
}

// অ্যারো ফাংশন
ধ্রুবক যোগ = (ক, খ) => ক + খ;

// অ্যাসিঙ্ক ফাংশন
অ্যাসিঙ্ক অনুষ্ঠান ডাটা_আনো() {
    ধ্রুবক ফল = অপেক্ষা fetch(url);
    প্রেরণ অপেক্ষা ফল.json();
}
```

---

## 🏗️ ক্লাস (Classes)

```banglascript
ক্লাস মানুষ {
    নির্মাতা(নাম, বয়স) {
        এই.নাম = নাম;
        এই.বয়স = বয়স;
    }
    
    পরিচয়() {
        লিখো("নাম: " + এই.নাম);
    }
}

// ইনহেরিটেন্স
ক্লাস ছাত্র বিস্তৃত মানুষ {
    নির্মাতা(নাম, বয়স, রোল) {
        সুপার(নাম, বয়স);
        এই.রোল = রোল;
    }
}

// অবজেক্ট তৈরি
ধ্রুবক ব্যক্তি = নতুন মানুষ("রহিম", ২৫);
```

---

## 📝 অ্যারে (Arrays)

```banglascript
ধ্রুবক তালিকা = [১, ২, ৩, ৪, ৫];

// মেথডস
তালিকা.ঠেলো(৬);           // push - শেষে যোগ
তালিকা.তোলা();            // pop - শেষ থেকে বের
তালিকা.দৈর্ঘ্য;            // length
তালিকা.প্রতিটিতে(fn);      // forEach
তালিকা.ম্যাপ(fn);          // map
তালিকা.ফিল্টার(fn);        // filter
তালিকা.খুঁজে(fn);          // find
তালিকা.অন্তর্ভুক্ত(৩);      // includes
তালিকা.সাজাও();           // sort
তালিকা.উল্টাও();          // reverse
তালিকা.জোড়া(",");         // join
```

---

## 📄 অবজেক্ট (Objects)

```banglascript
ধ্রুবক ব্যক্তি = {
    নাম: "রহিম",
    বয়স: ২৫,
    শহর: "ঢাকা"
};

// অ্যাক্সেস
ব্যক্তি.নাম           // রহিম
ব্যক্তি["বয়স"]        // 25

// JSON
JSON.stringify(obj)  // অবজেক্ট → স্ট্রিং
JSON.parse(str)      // স্ট্রিং → অবজেক্ট
```

---

## 🖥️ কনসোল (Console)

```banglascript
লিখো("বার্তা");           // console.log
ত্রুটি_লিখো("ত্রুটি");     // console.error
লিখো_সতর্কতা("সতর্কতা");  // console.warn
পরিষ্কার_করো();          // console.clear
```

---

## 🌐 DOM

```banglascript
// সিলেক্ট
দস্তাবেজ.সিলেক্টর_দ্বারা_পাও("#id")
দস্তাবেজ.আইডি_দ্বারা_পাও("id")
দস্তাবেজ.শ্রেণী_দ্বারা_পাও("class")

// পরিবর্তন
element.টেক্সট_পরিবর্তন_করো = "নতুন"
element.বিষয়বস্তু_পরিবর্তন_করো = "<b>HTML</b>"
element.শৈলী.রঙ = "blue"

// ইভেন্ট
element.ইভেন্ট_যোগ_করো("click", fn)

// ক্লাস
element.ক্লাস_যোগ_করো("active")
element.ক্লাস_মুছো("active")
element.ক্লাস_টগল_করো("active")

// এলিমেন্ট তৈরি
দস্তাবেজ.তৈরি_করো("div")
parent.যোগ_করো(child)
```

---

## ⚠️ এরর হ্যান্ডলিং

```banglascript
চেষ্টা {
    // ঝুঁকিপূর্ণ কোড
} ধরো (ত্রুটি) {
    লিখো(ত্রুটি.message);
} অবশেষে {
    // সবসময় চলবে
}

// এরর থ্রো
ছুড়ে_দাও নতুন Error("সমস্যা!");
```

---

## 🔢 Math

```banglascript
এলোমেলো_সংখ্যা()        // Math.random (0-1)
গণিত.বর্গমূল(১৬)       // Math.sqrt → 4
গণিত.শক্তি(২, ৩)       // Math.pow → 8
গণিত.পরম(-৫)          // Math.abs → 5
গণিত.সর্বোচ্চ(১,৫,৩)    // Math.max → 5
গণিত.সর্বনিম্ন(১,৫,৩)   // Math.min → 1
গণিত.রাউন্ড(৪.৫)       // Math.round → 5
গণিত.সিলিং(৪.১)       // Math.ceil → 5
গণিত.ফ্লোর(৪.৯)        // Math.floor → 4
```

---

## 📜 স্ট্রিং (String)

```banglascript
ধ্রুবক str = "হ্যালো বিশ্ব";

str.দৈর্ঘ্য              // length
str.বড়হাতের()          // toUpperCase
str.ছোটহাতের()          // toLowerCase
str.ছাঁটাই()            // trim
str.ভাগ(" ")           // split
str.প্রতিস্থাপন("a","b") // replace
str.খণ্ড(০, ৫)         // slice
str.সূচকে("বিশ্ব")       // indexOf
```

---

## 🔧 CLI কমান্ড

```bash
# ইন্সটল
npm install -g banglascript

# নতুন প্রজেক্ট
bjs init my-project
bjs init --web my-web
bjs init --api my-api

# বিল্ড ও রান
bjs build file.bjs -o build
bjs run file.bjs
bjs watch file.bjs

# অন্যান্য
bjs repl              # ইন্টার‍্যাক্টিভ
bjs keywords          # কীওয়ার্ড দেখুন
bjs --help            # সাহায্য
bjs --version         # ভার্সন
```

---

## 📁 প্রজেক্ট স্ট্রাকচার

```
my-project/
├── src/
│   └── main.bjs      # সোর্স কোড
├── build/            # কম্পাইল্ড JS
├── package.json
├── .gitignore
└── README.md
```

---

## 🔢 বাংলা সংখ্যা

| বাংলা | ইংরেজি |
|-------|--------|
| ০ | 0 |
| ১ | 1 |
| ২ | 2 |
| ৩ | 3 |
| ৪ | 4 |
| ৫ | 5 |
| ৬ | 6 |
| ৭ | 7 |
| ৮ | 8 |
| ৯ | 9 |

---

## 🎯 মনে রাখার বিষয়

1. **`নাহলে যদি`** ব্যবহার করুন (স্পেস সহ), `নাহলে_যদি` নয়
2. **`চেষ্টা`** ব্যবহার করুন (try এর জন্য)
3. **`অপেক্ষা`** ব্যবহার করুন (await এর জন্য)
4. **`প্রেরণ`** ব্যবহার করুন (return এর জন্য)
5. স্ট্রিং concatenation এ `+` ব্যবহার করুন

---

**🇧🇩 বাংলায় কোড লিখুন!**
